[pytest]
testpaths = test
python_files = test_*.py
norecursedirs = mcp/examples .git .pytest_cache .conda
markers =
    unit: fast, isolated tests with no external services or network dependencies
    integration: tests that exercise multiple subsystems or require local services/filesystem orchestration
    external: tests that require network access, real API keys, or third-party services
