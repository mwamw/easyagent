# 化学垂直领域 LLM 研究现状（整理与补充）

## 1. 发展脉络：从化学 PLM 到通用基座适配

在大规模自回归解码器成为主流之前，化学智能研究主要依赖专门预训练语言模型（PLM），如 MolT5、Text2Mol、MOLGEN、ChemBERTa 等。这类模型通常建立在 BERT 或 Seq2Seq（BART/T5）架构上，验证了“分子字符串可语言化建模”的可行性。

但传统化学 PLM 存在三类共性瓶颈：

1. 任务迁移成本高：新任务往往需要重新微调。
2. 泛化边界明显：零样本与跨任务推理能力不足。
3. 工具协同能力弱：难以稳定调用检索、计算、仿真等外部科学工具。

因此，研究范式已从“化学专用小模型”转向“通用大模型 + 化学领域持续适配”，即在 LLaMA、Mistral、Galactica 等开源基座上进行持续预训练（Continued Pre-training）和监督微调（SFT），以较低成本获得更强的跨任务能力。

## 2. 当前主流技术路线

### 2.1 数据驱动对齐

化学垂直 LLM 的核心竞争力越来越取决于数据工程，而不仅是参数规模。典型做法包括：

1. 指令化改写：将性质预测、逆合成、反应预测、分子优化等任务统一成指令-响应格式。
2. 多源融合：整合论文、专利、数据库、教材和问答数据，提升知识覆盖。
3. 结构-文本联合：将 SMILES、图结构描述、实验条件与自然语言解释共同建模。

代表性数据工作包括 SMolInstruct、Mol-Instructions、MolOpt-Instructions 等，推动了多任务统一训练与对话式能力构建。

### 2.2 能力增强：RAG、工具调用与 Agent 化

近期研究不再满足于“仅会生成文本”，而是强调“可验证的化学推理闭环”：

1. RAG（检索增强生成）：先检索文献/数据库，再生成结论，降低幻觉。
2. Tool Use：调用 RDKit、量化计算接口、反应规则引擎等外部工具。
3. Agent Workflow：将“检索-假设-计算-反思-修正”串成可追踪流程，提高科研可用性。

### 2.3 多模态化

纯文本模型难以覆盖反应路线图、谱图和结构图解析，因此出现了 ChemVLM 等多模态化学模型。常见架构是 ViT-MLP-LLM，通过视觉编码器与语言模型对齐，在 ChemOCR、图谱理解、多模态推理等任务中表现更稳健。

## 3. 代表模型与系统（按能力形态）

1. 任务型模型：聚焦单一或少数任务，如性质预测、分子生成、反应预测。
2. 指令型模型：基于多任务指令数据训练，强调统一接口与迁移能力。
3. 对话型模型：如 ChemLLM，强调化学问答、教学交互与解释能力。
4. 多模态模型：如 ChemVLM，处理图像-文本联合推理场景。

其中，ChemLLM 类工作展示了“中文化学对话 + 任务能力融合”的方向；ChemVLM 则体现了从文本智能走向实验场景理解的趋势。

### 3.1 代表文献补充（含 DFM-R）

为便于写作“相关工作”章节，可将常见代表工作按以下脉络组织：

1. 通用科学/生化基座模型
	Galactica、BioMedLM、SciGLM 等工作证明了“科学语料持续预训练”对专业问答和知识表述有效，但在化学结构推理与反应规划上仍需专门适配。
2. 化学 PLM 与分子语言模型
	ChemBERTa、MolT5、MOLGEN、Text2Mol 等模型奠定了 SMILES 文本化、分子表示学习与生成建模基础，是后续化学 LLM 的先导路线。
3. 指令化与多任务化学 LLM
	LlaSMol 系列（基于 SMolInstruct）、Mol-Instructions、MolOpt-Instructions 等工作推动了“统一指令接口 + 多任务学习”范式，使模型具备更好的跨任务迁移能力。
4. 对话式化学模型
	ChemLLM 代表了“化学知识库 + 对话模板 + 领域对齐”的路线，强调中文场景下的问答交互、解释能力与任务实用性。
5. 多模态化学模型
	ChemVLM 等工作通过 ViT-MLP-LLM 架构融合图像与文本，覆盖 ChemOCR、谱图理解、结构图解析等纯文本模型难以胜任的场景。
6. 专用大模型路线（重点补充）
	DFM-R 可归入“面向反应/反应推理的专用大模型”代表方向：相比通用对话模型，其设计更关注反应条件、产物路径、步骤约束与可执行性评估。在文献综述中建议将 DFM-R 与通用化学 LLM 分开讨论，突出其在“任务专用性、结构约束建模、实验可落地性”方面的定位。

如果你后续要写成正式论文版本，建议在这一小节按“模型名称-训练数据-核心任务-评测基准-主要结论”五列制作表格，DFM-R 可以放在“反应专用模型”子类中与通用模型做横向对比。

## 4. 评测体系与研究焦点

当前评测正从“单点分数”转向“科研可用性”评估，重点包括：

1. 任务性能：分子性质预测、反应产物预测、逆合成规划、分子优化。
2. 推理质量：多步推理一致性、错误自检和不确定性表达。
3. 事实可靠性：文献依据可追溯性、化学知识正确率、幻觉率。
4. 工具协同：调用外部工具后的成功率、端到端任务完成率。
5. 安全与合规：危险化学知识输出约束、实验建议风险控制。

## 5. 主要挑战（截至 2026 年）

1. 高质量指令数据稀缺：化学专家标注成本高，噪声样本影响模型边界。
2. 评测标准尚未统一：不同论文任务定义和数据切分不一致，横向比较困难。
3. 可解释性不足：模型给出结论但缺少可复现的机理级证据链。
4. 幻觉与过度自信：在“看似合理”的化学答案中仍可能隐藏关键错误。
5. 从离线评测到湿实验闭环仍有鸿沟：真实实验成本与安全要求远高于基准测试。

## 6. 未来趋势判断

1. 从“模型中心”走向“系统中心”：LLM 将与知识库、仿真器、实验自动化平台深度耦合。
2. 从“文本问答”走向“科研执行”：更强调规划、验证、迭代的 Agent 能力。
3. 从“单模态理解”走向“多模态实验智能”：融合谱图、图像、表格、实验日志。
4. 从“泛化能力”走向“可信能力”：可追溯证据、可复现实验路径、风险可控输出将成为核心指标。

## 7. 可用于论文/报告的简要结论

化学垂直领域 LLM 已进入“通用基座适配 + 领域数据工程 + 工具增强 + 多模态融合”的阶段。短期内，性能提升仍依赖高质量数据与训练策略；中长期的关键竞争点将转向“可验证推理、实验闭环对接与安全可信部署”。