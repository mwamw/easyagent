# EasyAgent 依赖

# 核心依赖
openai>=1.0.0
pydantic>=2.0.0
typing_extensions>=4.0.0
python-dotenv>=1.0.0

# RAG 模块
numpy>=1.24.0
chromadb>=0.4.0
markitdown>=0.1.0

# PDF 加载 (可选)
pypdf>=3.0.0

# 编码检测 (可选)
chardet>=5.0.0

# Token 分割 (可选)
tiktoken>=0.5.0

# 搜索工具 (可选)
google-search-results>=2.4.0

# MCP 协议工具 (可选)
fastmcp>=2.0.0

# RAG 高级功能 (可选)
# rank-bm25>=0.2.0          # BM25 检索
# jieba>=0.42.0             # 中文分词
# sentence-transformers>=2.0.0  # HuggingFace 嵌入模型
